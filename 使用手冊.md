# RADIUS Lab 網站・使用手冊

這份手冊教你**在哪裡改什麼**、怎麼更新、怎麼上線。你幾乎不用碰 HTML／CSS——
日常維護只要編輯**兩個資料檔**,再按一個鍵重建即可。

---

## 目錄
1. [30 秒快速上手](#1-30-秒快速上手)
2. [檔案總覽:哪個檔做什麼](#2-檔案總覽)
3. [核心觀念:資料 → 重建 → 網頁](#3-核心觀念)
4. [日常維護四步驟(在 VSCode 裡)](#4-日常維護四步驟)
5. [內容編輯地圖(每個區塊改哪裡)](#5-內容編輯地圖)
6. [常見任務・食譜](#6-常見任務食譜)
7. [加入照片](#7-加入照片)
8. [換顏色與字型](#8-換顏色與字型)
9. [發佈到 GitHub](#9-發佈到-github)
10. [黃金守則與禁忌](#10-黃金守則與禁忌)
11. [疑難排解](#11-疑難排解)

---

## 1. 30 秒快速上手

要改網站內容,你**永遠只做這件事**:

```
① 開 content_en.json（英文）和 content_zh.json（中文），改文字
② 按 ▶ 執行 build.py        → 產生最新的網頁
③ 用瀏覽器看 index.html      → 確認沒問題
④ 在 VSCode 送出（Commit + Sync）→ 約一分鐘後網站更新
```

網址:**https://radiusncku.github.io/**

---

## 2. 檔案總覽

| 檔案 | 作用 | 你要動它嗎 |
|------|------|-----------|
| **`content_en.json`** | 全站**英文**內容 | ✏️ 常編輯 |
| **`content_zh.json`** | 全站**中文**內容 | ✏️ 常編輯 |
| `build.py` | 讀兩個資料檔 → 產生所有網頁 | ▶ 用「執行」的,不用改 |
| `check_bilingual.py` | 檢查中英文有沒有對齊 | ▶ 用「執行」的 |
| `template.html` | 網頁外殼(`<head>`、字型) | 幾乎不動 |
| `styles.css` | 顏色、字型、版面 | 偶爾(§8) |
| `script.js` | 語言切換、手機選單、動畫 | 幾乎不動 |
| `index.html` 等 7 個 `.html` | **自動產生的成品** | ⚠️ **不要手改**,每次 build 會覆蓋 |
| `使用手冊.md` | 這份手冊 | 參考用 |

自動產生的 7 個頁面:`index.html`(首頁)、`pi.html`(主持人)、`people.html`(成員)、
`publications.html`(著作)、`teaching.html`(教學)、`resources.html`(資源)、`news.html`(消息)。

> ⚠️ **千萬不要直接改那 7 個 `.html`**。它們是機器產生的,你下次一 build 就會被蓋掉。
> 要改內容,一律改 `content_en.json` / `content_zh.json`。

---

## 3. 核心觀念

網站是「**資料**」和「**外觀**」分開的:

```
content_en.json ┐
content_zh.json ┤─→ (python build.py) ─→ index.html / pi.html / … 7 個網頁
template.html   ┘
                       ↑ 外觀由 styles.css 決定
```

- 你維護的是**資料**(JSON,像 Python 字典)。
- `build.py` 幫你把資料套進版型,產生網頁。
- **兩個 JSON 檔必須「長得一樣」**(相同的欄位、相同的清單長度)。
  只是同一個欄位:英文檔放英文、中文檔放中文。
- 如果你只在英文檔加了一筆卻忘了中文檔,`build.py` 會**直接停下來**告訴你哪裡對不上,
  不會產生壞掉的網站。這就是你的安全網。

**哪些欄位兩邊要「一模一樣」、哪些要「各放各的語言」?**

- 兩邊**填一樣**(與語言無關):年份 `year`、日期 `date`、學期 `term`、連結網址 `href`、
  信箱 `email`、照片路徑 `photo`、在學年份 `cohort`、**論文標題/作者/期刊**(學術名稱不翻譯)。
- 兩邊**各放各語言**:各種 `title`/`desc`/`text`/`bio`/`note`、人名 `name`、職稱 `role`。

---

## 4. 日常維護四步驟

全部在 VSCode 裡完成,主要靠點按鈕。

**① 改內容** — 左邊點開 `content_en.json` / `content_zh.json`,改文字。

**② 重建** — 點開 `build.py`,按右上角 **▶ Run**。
下方看到 `DONE. All pages built.` 就成功;
若哪裡少了翻譯,它會用白話告訴你(例如「英文有 5 筆但中文只有 4 筆」),改好再按一次。

**③ 預覽** — 對 `index.html` 按右鍵 → **Show Preview**(或用瀏覽器開),
點各頁與 **中文/EN** 看看。

**④ 上線** — 左側**版本控制**面板(分支圖示,或 `Ctrl/Cmd+Shift+G`):
打一句說明(如「更新六月消息」)→ 按 **✓ Commit** →
按 **Sync Changes**。約一分鐘後 https://radiusncku.github.io/ 就更新了。

> ⚠️ Commit 時,如果你**新增了頁面或照片**,記得把那些**新檔案也一起勾選**再送出,
> 否則新東西不會上線。

---

## 5. 內容編輯地圖

以下告訴你每個區塊在 JSON 的哪個位置。**路徑**用點表示層級,例如
`people.groups[1].members` 代表:`people` 物件 → `groups` 清單的第 2 筆 → 它的 `members`。
(清單從 0 開始數:`[0]` 是第一筆。)

### 5.1 網站基本資訊 — `meta`
分頁標題與搜尋描述。`meta.title`、`meta.description`。

### 5.2 導覽列文字 — `nav`
各按鈕的文字:`about / research / pi / people / publications / teaching / resources / news / join`,
以及語言鈕 `lang_button`。**只改文字**,連到哪頁是固定的。

### 5.3 首頁大標區 — `hero`
- `hero.eyebrow`:最上面那行小標(RADIUS 縮寫)。
- 大標題拆三段:`title_before` + `title_accent`(紅字)+ `title_after`。
  例:英文 `"Engineering "` + `"Resilience"` + `"."`。
- `hero.subtitle`:標題下的一句話。
- `hero.btn_primary` / `hero.btn_secondary`:兩顆按鈕文字。

### 5.4 關於 — `about`
`about.eyebrow`、`about.title`、`about.p1`(第一段)、`about.p2`(第二段)。

### 5.5 研究方向 — `research.items`(清單)
每筆:`{ "num": "01", "title": "…", "desc": "…" }`。
`num` 兩邊一樣;`title`/`desc` 各放語言。要增減主軸就加/刪整筆(**兩個檔都要**)。

### 5.6 主持人 — `pi`
- `pi.name`(英文檔英文名/中文檔中文名)、`pi.role`、`pi.bio`。
- `pi.photo`:照片路徑(見 §7),留 `""` 顯示灰框。
- `pi.links`:清單 `{ "label": "Google Scholar", "href": "https://…" }`。
- `pi.interests`:**字串清單**(研究興趣標籤),例如 `["Urban resilience", …]`。
- `pi.education`:清單 `{ "years": "", "degree": "…", "place": "…" }`。
- `pi.experience`:清單 `{ "years": "", "role": "…", "place": "…" }`。
- 對應的標題:`interests_title` / `education_title` / `experience_title`。

### 5.7 成員 — `people.groups`(4 組)
四組固定:`groups[0]`=博士生、`groups[1]`=碩士生、`groups[2]`=大學部、`groups[3]`=畢業生。
每組 `members` 是清單,每位成員:

```json
{ "photo": "", "name": "…", "cohort": "202X–202X", "note": "…" }
```

- `name`:**英文檔放英文名、中文檔放中文名**。只有一種時,另一邊留 `""`,
  網站會自動顯示有的那個(所以只有中文名的同學,英文版也會顯示中文名)。
- `cohort`:在學年份(兩邊一樣),例如 `"2024–2026"`。
- `note`:個人簡介一句(各放語言);畢業生就填「目前職稱與服務單位」。
- 空的組(大學部/畢業生)**不會顯示**,有人再加就會自動出現。

### 5.8 著作 — `publications.groups`(3 組)
`groups[0]`=期刊論文、`groups[1]`=會議論文與海報、`groups[2]`=技術報告。
每組 `items` 每筆:

```json
{
  "year": "2025",
  "title": "論文標題(英文,兩邊一樣,不翻譯)",
  "authors": "C.-W. Hsu, … & A. Mostafavi",
  "journal": "期刊名",
  "links": [ { "label": "DOI", "href": "https://doi.org/…" } ]
}
```

`links` 可放多個(PDF / Code / DOI);沒有連結就寫 `"links": []`。
底部「查看全部」連到 `publications.more_href`(你的 Google Scholar)。

### 5.9 教學與邀請演講 — `teaching`
- `teaching.courses`:每筆 `{ "term": "Fall 2025", "title": "課程名稱" }`。
- `teaching.talks`:每筆 `{ "date": "2026/06/18", "title": "講題", "venue": "主辦單位" }`。
  `title`/`venue` 各放語言,`date` 兩邊一樣。

### 5.10 資源 — `resources.items`(目前為空)
每筆 `{ "title": "…", "desc": "…", "href": "https://…" }`。
清單空的時候頁面顯示「即將更新」。

### 5.11 消息 — `news.items`
每筆 `{ "date": "2026.06", "text": "一句話" }`。最新的放最上面。

### 5.12 加入我們 — `join`
`join.title`、`join.text`、`join.email`、`join.note`(招生訊息)。

### 5.13 頁尾 — `footer`
`footer.address`(用 `\n` 換行)、`explore_links`、`elsewhere_links`(隸屬單位連結)、`copyright`。

---

## 6. 常見任務・食譜

> 通則:**複製一筆現有的、貼上、改文字**,而且**兩個 JSON 檔都要改**。

### 新增一位碩士生
在 `content_en.json` 的 `people.groups[1].members` 複製一筆,改成:
```json
{ "photo": "", "name": "Wang Xiaoming", "cohort": "2025–2027", "note": "Graduate researcher at the RADIUS Lab." }
```
在 `content_zh.json` 相同位置加對應中文:
```json
{ "photo": "", "name": "王小明", "cohort": "2025–2027", "note": "RADIUS 實驗室研究生。" }
```

### 新增一篇期刊論文
在兩個檔的 `publications.groups[0].items` 最前面加一筆(標題/作者/期刊兩邊填一樣,英文):
```json
{ "year": "2026", "title": "…", "authors": "C.-W. Hsu et al.", "journal": "…",
  "links": [ { "label": "DOI", "href": "https://doi.org/…" } ] }
```

### 新增一則消息
兩個檔的 `news.items` 最前面加一筆,`date` 一樣、`text` 各放語言。

### 換信箱
搜尋 `chiaweihsu@gs.ncku.edu.tw`,兩個檔都改(出現在 `join.email` 與 `pi.links` 的 Email)。

### 新增一個資源連結
兩個檔的 `resources.items` 加 `{ "title": "…", "desc": "…", "href": "https://…" }`。

### 補上學生的英文拼音 / 在學年份
到 `content_en.json` 對應成員把 `name` 從 `""` 填成英文拼音;`cohort` 兩邊改成真實年份。

改完記得:**▶ build → 預覽 → Commit + Sync**。

---

## 7. 加入照片

這是靜態網站,沒有「上傳鈕」——把圖檔放進資料夾、在 JSON 寫檔名即可。

1. 在網站資料夾開一個 `images/` 子資料夾,把圖放進去(如 `images/joe.jpg`)。
   檔名用小寫、不要空格;格式 `.jpg`/`.png`/`.webp`;上傳前壓到長邊約 800–1200px、200KB 內。
2. 在 JSON 把 `photo` 從 `""` 改成路徑,例如主持人:
   ```json
   "photo": "images/joe.jpg"
   ```
   成員照片同理,改該成員的 `photo`(建議兩個檔填一樣)。
3. ▶ build。照片會自動裁切填滿方框、不變形。
4. **Commit 時務必把 `images/` 資料夾一起送出**,網站才找得到圖。

---

## 8. 換顏色與字型

打開 `styles.css`,最上面 `:root` 區塊就是全站調色盤,改一個值全站更新:

```css
--phoenix: #E8442F;   /* 橘紅主色(強調) */
--maroon:  #6E1E2E;   /* 成大深紅(次色) */
--cream:   #FAF7F4;   /* 淺色背景 */
--ink:     #1A1416;   /* 深色區塊(導覽列、Hero) */
```

`#E8442F` 是十六進位色碼,用任何線上取色器挑新色貼上即可。
字型在其下方 `--font-*`,建議別動。改完直接存檔即可(不需要 build,styles.css 不是產生出來的)。

---

## 9. 發佈到 GitHub

日常更新只要在 VSCode 版本控制面板 **Commit → Sync**(見 §4 第 ④ 步)。約一分鐘後
https://radiusncku.github.io/ 自動更新。

第一次設定(只做一次)之前已完成:倉庫 `radiusncku/radiusncku.github.io`、
Settings → Pages 已開啟。之後都不用再設定。

> 提醒:**新增檔案(新頁面、`images/` 裡的圖)** 要在 Commit 時一起勾選,否則不會上線。

---

## 10. 黃金守則與禁忌

1. **只改 `content_en.json` / `content_zh.json`**;不要手改那 7 個 `.html`。
2. **兩個語言檔要同步**:加一筆就兩邊都加,結構保持一致。`build.py` 會幫你把關。
3. **每次上線前先 ▶ build 再預覽**,看到 `DONE` 且畫面正常再送出。
4. **JSON 語法要對**:逗號、引號要成對;最後一筆後面不要多逗號。
5. 版本控制就是你的備份:送出過的版本都留著,隨時可回溯(見 §11)。
6. 資料檔用**底線**命名:`content_en.json` / `content_zh.json`。`build.py` 會自動尋找
   資料檔——底線版找不到才找點號版(`content.en.json`),所以兩種命名都能正常讀取,
   你不用擔心檔名對不上。

---

## 11. 疑難排解

**執行 build.py 報錯 `not valid JSON`**
→ 某個 JSON 檔打錯字(多/少逗號、引號沒成對)。錯誤訊息會告訴你**行號**,去那行修。

**build.py 說「英文 N 筆、中文 M 筆」對不上**
→ 你在其中一個語言檔加/刪了一筆卻忘了另一邊。把兩邊補成一樣。

**切到英文版,某位學生顯示的是中文名**
→ 那位的英文 `name` 還是 `""`。到 `content_en.json` 幫他填上英文拼音。

**切換語言時某行文字消失**
→ 兩個檔的結構沒對齊。執行 `python check_bilingual.py *.html`,它會指出哪一頁、哪一行。

**改了資料,網站沒更新**
→ ①有沒有 ▶ build? ②有沒有 Commit + Sync? ③等 1–2 分鐘,再用
`Ctrl/Cmd+Shift+R` 強制重新整理瀏覽器。

**照片沒出現**
→ ①`photo` 路徑和實際檔名(含大小寫)要完全一致;②`images/` 資料夾有沒有一起 Commit。

**新頁面 404 打不開**
→ 該 `.html` 檔沒被 Commit 上去。回 GitHub 倉庫確認檔案在不在。

**想還原成之前的版本**
→ GitHub 會保留每次送出的版本。到倉庫點該檔 → History,找可用的版本還原。

---

## 快速檢查指令(選用)

在網站資料夾的終端機(或 VSCode 內建終端機)可執行:

```bash
python build.py                # 重建全部頁面(等同按 ▶）
python check_bilingual.py *.html   # 檢查所有頁面中英文是否對齊
```

---

*維護心法一句話:**複製現有的一筆 → 兩個語言檔都改 → ▶ build → 預覽 → 送出**。*
